# pnpm + Expo/React Native : Metro a besoin d'un node_modules « hoisté » (à plat).
# Sans ça, certains modules natifs ne sont pas résolus.
node-linker=hoisted

# Miroir npmmirror (le registre npmjs.org est instable/bloqué sur ce réseau).
registry=https://registry.npmmirror.com/
